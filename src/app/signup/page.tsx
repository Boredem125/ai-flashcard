// This page no longer exists.
import { notFound } from 'next/navigation';

export default function SignupPage() {
  notFound();
  // This return is technically unreachable but good for linters/TS
  return null;
}
